/**
 * 业务组件库 - Business Components
 * 
 * 这是项目特定的业务组件库，包含与特定业务逻辑相关的复杂组件。
 * 业务组件基于公共组件库(common)构建，可能涉及数据获取、状态管理等。
 * 
 * 使用指南：
 * 1. 业务组件应该专注于特定的业务场景
 * 2. 可以直接依赖业务逻辑和API调用
 * 3. 应该避免过度的组件嵌套
 * 4. 当业务组件变得通用时，考虑萃取到common组件库
 * 
 * 当前结构：
 * - 预留位置，待业务组件逐步迁移和集中
 * - 现有的业务组件在各自的功能目录下（如 chat/）
 */

// 这里导出业务相关的复杂组件
// 例如：
// export { ChatMessage } from './ChatMessage';
// export { UserProfile } from './UserProfile';
