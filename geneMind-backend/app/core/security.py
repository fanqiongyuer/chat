class EncryptionUtil:
    """加密占位类：当前透传，后续可替换为真实加解密实现。"""

    @staticmethod
    def encrypt_text(plain_text: str) -> str:
        return plain_text

    @staticmethod
    def decrypt_text(cipher_text: str) -> str:
        return cipher_text
